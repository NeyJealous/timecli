# Generated PortCore plugin

Run `tools/unity/sync-portcore.ps1` or `sync-portcore.sh`.

The scripts build `src/PortCore` for `netstandard2.1` and copy:

`TimeClickers.PortCore.dll`

into this directory.

The DLL is generated output and is ignored by Git.
